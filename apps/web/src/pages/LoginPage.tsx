export default function LoginPage() {
  return (
    <main className="auth-page">
      <section className="placeholder-panel">
        <p className="eyebrow">Authentication</p>
        <h1>Login</h1>
        <p>This placeholder page will contain the CampusFlow login form later.</p>
      </section>
    </main>
  );
}
